Generate a Python module named logger.py.

Purpose:
Provide structured JSON logging for all modules.

Requirements:
- Functions:
  - log_info(module, message, data)
  - log_error(module, message, data)
- Output JSON with:
  - timestamp
  - module
  - level
  - message
  - data

Technical Requirements:
- No external dependencies
- Add unit tests for log formatting
